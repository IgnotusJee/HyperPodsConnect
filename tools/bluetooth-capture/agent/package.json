{
  "name": "bluetooth-capture-agent",
  "version": "1.0.0",
  "description": "Observe-only Frida agent for generic Bluetooth transport tracing",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "frida-compile agent.ts -o ../_agent.js -c",
    "watch": "frida-compile agent.ts -o ../_agent.js -w"
  },
  "license": "ISC",
  "dependencies": {
    "frida-java-bridge": "^7.0.13"
  },
  "devDependencies": {
    "@types/frida-gum": "^19.10.0",
    "frida-compile": "^19.0.5",
    "typescript": "^7.0.2"
  }
}
