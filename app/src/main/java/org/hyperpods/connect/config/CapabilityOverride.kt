package org.hyperpods.connect.config

/** Stable persistence values for an automatic/forced capability override. */
object CapabilityOverride {
    const val AUTO = 0
    const val FORCE_ENABLED = 1
    const val FORCE_DISABLED = 2
}
